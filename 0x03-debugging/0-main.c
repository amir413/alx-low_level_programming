#include <stdio.h>

int main()
{
    int a;
    if (a > 0)
    {
        printf ( a ,"%d number is postive");
    }
    else if (a < 0)
    {
        printf ( a ,"%d number is negative");
    }
    else
    {
        printf ( a ,"%d is zero");
    }

    return 0;
}

