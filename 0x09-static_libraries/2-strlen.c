#include "main.h"
#include <string.h>

/**
 * _strlen - Calculates the length of a string
 * @s: Pointer to a string
 *
 * Return: Length of the string
 */
int _strlen(char *s)
{
	int st;

	st = strlen(s);
	return (st);
}
